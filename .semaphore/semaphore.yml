version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: checkout
    task:
      jobs:
        - name: checkout
          commands:
            - checkout
        - name: 'Job #4'
          commands: []
        - name: lint
          commands:
            - echo lint
        - name: yarn install
          commands:
            - echo install
      env_vars:
        - name: FOO_1
          value: BAR_1
    dependencies: []
  - name: Client
    dependencies:
      - checkout
    task:
      jobs:
        - name: lint
          commands:
            - echo $FOO_1
  - name: Server
    dependencies:
      - checkout
    task:
      jobs:
        - name: lint
          commands:
            - ls
